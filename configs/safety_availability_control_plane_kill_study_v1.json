{
  "base_commit": "3504852cdebc3d430a812f7ee67ba873096daa22",
  "completion_rule": "Report both routing/bypass and load/escalation results, compare all three control strategies, evaluate the formal necessary conditions, and issue an internal go/no-go signal without making an external literature-novelty claim.",
  "formal_necessity_conditions": {
    "finite_budget": "If N accepted requests all require expensive evaluation with per-request expensive cost c_e > 0, then accept-all fail-closed service requires B >= N*c_e. If B < N*c_e, an explicit non-service action or worst-case provisioning is necessary.",
    "queue_stability": "If escalated arrival rate q*lambda > expensive service rate mu, an accept-all fail-closed queue has positive backlog drift. Explicit admission control, defer/reject, or provisioning for that worst-case rate is necessary."
  },
  "internal_novelty_kill_rule": {
    "continue_signal": "At perturbation <= the frozen Step-3 dead-band, at least 1% of full-caught unsafe development examples become newly bypassable in at least two stacks.",
    "deadband_scale_incremental_bypass_threshold": 0.01,
    "literature_novelty_claim": false,
    "minimum_stacks_for_nontrivial_signal": 2,
    "otherwise": "Treat overload/resource failure as the generic capacity/admission-control condition rather than sufficient evidence for a standalone security paper."
  },
  "load_escalation_pressure": {
    "capacity_source": "reports/evaluation_measurement_pilot_v1/t4/policy_latency_summary.csv",
    "expensive_service_time": "direct_mean_ms(full_information) - direct_mean_ms(cheap_only)",
    "nominal_expensive_utilization": 0.8,
    "single_t4_expensive_service_rate": "1000 / expensive_increment_ms",
    "stability_condition": "In the deterministic fluid-capacity model, q*lambda <= mu has no positive backlog drift; q*lambda > mu is overloaded. Stochastic deployment requires headroom.",
    "strategies": [
      "fail_open_budget_cap",
      "fail_closed_no_admission",
      "fail_closed_defer_reject"
    ],
    "total_arrival_load_multipliers": [
      0.5,
      1.0,
      1.25,
      1.5,
      2.0
    ]
  },
  "numerical_fail_closed_rule": {
    "ambiguous_route_action": "escalate",
    "note": "This study consumes the completed Step-3 dead-band result. It does not retune the dead-band.",
    "route_deadband_epsilon": 1e-06,
    "source": "reports/numerical_route_stability_v1/final/summary.json"
  },
  "ordered_prerequisites": {
    "factorial_measurement_decomposition_complete": true,
    "numerical_route_stability_complete": true,
    "provenance_reproducibility_complete": true
  },
  "primary_design": {
    "policy_kind": "selective_deployable",
    "primary_stack": "rule_compact_to_qwen",
    "reason": "Use one prespecified mid-rate selective policy per existing stack; the three-monitor rule_compact_to_qwen stack is primary and the other two stacks are sensitivities. No policy is selected from Step-4 outcomes.",
    "sensitivity_stacks": [
      "compact_to_qwen",
      "rule_to_compact"
    ],
    "target_rate": 0.25
  },
  "protocol_id": "safety_availability_control_plane_kill_study_v1",
  "purpose": "Small development-only novelty-kill study of whether a fail-closed selective safety cascade can preserve its safety semantics and bounded resource use under routing/bypass pressure and load/escalation pressure, or whether explicit defer/reject/admission control becomes necessary.",
  "routing_bypass_pressure": {
    "attack_direction": "For unsafe development examples caught by the frozen full model but missed by the frozen cheap model, move only the routing probability toward the cheap-safe, unambiguously non-escalated side.",
    "perturbation_grid": [
      0.0,
      1e-06,
      0.0001,
      0.001,
      0.01,
      0.05,
      0.1,
      0.25
    ],
    "primary_metric": "incremental_full_caught_unsafe_bypassable_fraction",
    "realizability_claim": false,
    "route_score_only_counterfactual": true,
    "type": "synthetic_route_probability_stress"
  },
  "routing_escalation_pressure": {
    "attack_direction": "Move routing probability toward the fail-closed escalation region; report the fraction of development requests that can be forced to escalate at each perturbation budget.",
    "perturbation_grid": [
      0.0,
      1e-06,
      0.0001,
      0.001,
      0.01,
      0.05,
      0.1,
      0.25
    ],
    "type": "synthetic_route_probability_stress",
    "worst_case_escalation_fraction": 1.0
  },
  "scope": {
    "development_only": true,
    "fresh_confirmatory_claim": false,
    "new_model_training": false,
    "pareto_claim_available": false,
    "production_claim": false,
    "protected_legacy_splits_used": false,
    "router_superiority_claim": false,
    "router_tuning_or_rescue": false,
    "standalone_security_paper_claim": false
  },
  "status": "frozen_before_outcomes"
}
