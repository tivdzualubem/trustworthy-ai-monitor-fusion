# Curated Results

This folder contains the main figures, tables, and metadata for reviewing the current project progress.

The detailed analysis reports remain in `reports/`.

Important interpretation: calibration bounds are in-distribution post-freeze summaries. The held-out GCG split is a shift diagnostic, not a robustness guarantee.
